# AGENTS.md

## Project shape
- Flutter app at repo root; main entrypoint is `lib/main.dart`.

## Forui theming
- `lib/theme/scaffold_style.dart` is generated by the Forui CLI; regenerate with `dart forui theme create [theme name]` if you need a fresh baseline instead of hand-editing the generated structure.
